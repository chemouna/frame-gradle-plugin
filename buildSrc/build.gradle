repositories {
  mavenCentral()
  maven {
    url "https://oss.sonatype.org/content/repositories/snapshots"
  }
}

rootProject.dependencies {
  compile project(':screenshots-plugin')
  //compile project(':frame-plugin')
}
